print(len([[1,2]]*3))

#if you use the index() method to find the index of a value that doesn’t exist in the list, it raises a ValueError

min(["a", "b", "c"])

[1, 2, 3].count("one")

max([1,2, "three"])#raise an error